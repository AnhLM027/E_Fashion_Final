server:
  port: 2000
  forward-headers-strategy: native

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:e_fashion}?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: minhanh131415@gmail.com
    password: ahlqnjurtdzezqma
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  access_expiration: 900
  refresh_expiration: 2592000
  secretKey: 2aATAiGKfFasIR0ppV8kwBTHbXQSjB7Vzb0zq0rId4Q=

file:
  upload-dir: ${UPLOAD_DIR:uploads/}